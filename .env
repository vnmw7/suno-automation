# Default configuration for Docker Compose
# Override these values in .env.local (see docs/setup_guide/docker-images-setup.md)

TAG=latest
CAMOUFOX_SOURCE=auto

# Uncomment and set these to use published images instead of local builds
# FRONTEND_IMAGE=your-namespace/suno-frontend:latest
# BACKEND_IMAGE=your-namespace/suno-backend:latest
