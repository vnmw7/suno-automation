# Frontend Docker Environment Configuration
# This file is used when running the frontend in Docker standalone mode

# API Configuration - Backend running on host
BACKEND_URL=http://host.docker.internal:8000
VITE_API_URL=http://localhost:8000

# Supabase Configuration (copy from your .env file)
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_KEY=your_supabase_key_here

# Environment
NODE_ENV=production
PORT=3000
HOST=0.0.0.0